"use client";

import CampsPage from "./camps/page";

export default function CoachDashboard() {
  // For now, just render the camps page
  // In the future, this could be enhanced to work with the sidebar navigation
  return <CampsPage />;
}
